{% extends 'base.html.twig' %}

{% block title %}Gestion des chatons BDD{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Gestion des chaton en BDD</h1>
        <table class="table table-striped">
            <tr>
                <th>Titre</th>
                <th>Description</th>
                <th></th>
                <th></th>
                <th></th>
            </tr>
            {% for c in categories %}
                <tr>
                    <td>{{ c.titre }}</td>
                    <td>{{ c.description }}</td>
                    <td>
                        <a class="btn btn-primary" href="{{ path("categorie_modifier", {"id":c.id}) }}">modifier</a>
                    </td>
                    <td>
                        <a class="btn btn-danger" href="{{ path("categorie_supprimer", {"id":c.id}) }}">supprimer</a>
                    </td>
                    <td>
                        <a class="btn btn-success" href="{{ path("voir_chatons", {"id":c.id}) }}">voir les chatons</a>
                    </td>
                </tr>
            {% endfor %}
        </table>

        <h2>Ajouter une catégorie</h2>
        {{ form(formulaire) }}


    </div>
{% endblock %}
